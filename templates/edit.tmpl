{{define "content"}}
	<form class="recipe-form" action="{{.SubmitURL}}" method="post">
		<div class="single-item-groups">
			<div class="form-group">
				<label for="recipe-name">Recipe Name:</label>
				<input type="text" id="recipe-name" name="recipe-name" value="{{.RecipeName}}" required>
			</div>
			<div class="form-group">
				<label for="author">Author:</label>
				<input type="text" id="author" name="author" value="{{.Author}}" required>
			</div>
			<div></div>
			<div class="form-group">
				<label for="prep-time">Prep time:  <span class="form-error-message">Error format: 00h00m00s</span></label>
				<input type="text" id="prep-time" name="prep-time" value="{{.PrepTime}}" pattern="(\d+h)?(\d+m)?(\d+s)?">
			</div>
			<div class="form-group">
				<label for="total-time">Total time:  <span class="form-error-message">Error format: 00h00m00s</span></label>
				<input type="text" id="total-time" name="total-time" value="{{.TotalTime}}" pattern="(\d+h)?(\d+m)?(\d+s)?">
			</div>
			<div class="form-group">
				<label for="yield">Yield:</label>
				<input type="text" id="yield" name="yield" value="{{.Yield}}" required>
			</div>
		</div>
		<div class="textarea-group">
			<label for="ingredients">Ingredients:</label>
			<span class="form-error-message" id="ingredient-error">Error (Invalid Formatting): Lines must begin with "## " (denoting a section header) or "* " (denoting an ingredient)</span>
			<textarea class="ingredient-area" id="ingredients" name="ingredients" required>{{.Ingredients}}</textarea>
			<script type="text/javascript" src="/assets/js/ingredient-area-validation.js"></script>
		</div>
		<div class="textarea-group">
			<label for="steps">Steps:</label>
			<textarea class="steps-area" id="steps" name="steps" required>{{.Steps}}</textarea>
		</div>
		<input type="submit" value="Submit">
	</form>
{{end}}
